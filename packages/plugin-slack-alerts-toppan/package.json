{
  "name": "@paperclipai/plugin-slack-alerts-toppan",
  "version": "0.1.0",
  "type": "module",
  "description": "Slack alerts for Toppan Security — issue assignments, failed runs, blocked tasks",
  "main": "./dist/worker.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@paperclipai/plugin-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  },
  "paperclipPlugin": {
    "manifest": "./dist/manifest.js"
  }
}
